sudo: required
dist: trusty
addons:
  postgresql: "9.5"
  apt:
    packages:
      - postgresql-9.5-postgis-2.3
      - build-essential

before_install:
  - psql -c "CREATE EXTENSION postgis"
  - git clone https://github.com/petere/plsh
  - cd plsh && make
  - cd plsh && sudo make install
  - psql -U postgres -c "CREATE EXTENSION plsh"
  
script:
  - make build
  - sudo make install
  - psql -U postgres -c "CREATE EXTENSION frapi"
  - psql -U postgres -c "SELECT * FROM frapi.adresse_search('route des anges, landéda')"
